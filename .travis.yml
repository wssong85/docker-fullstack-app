language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t tabliser/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true tabliser/react-test-app npm run test

after_success:
  - docker build -t tabliser/docker-frontend ./frontend
  - docker build -t tabliser/docker-backend ./backend
  - docker build -t tabliser/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push tabliser/docker-frontend
  - docker push tabliser/docker-backend
  - docker push tabliser/docker-nginx